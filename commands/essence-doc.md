---
description: Rewrite a doc/comment/note file to essence, preserving code/URLs/paths exactly
argument-hint: "<filepath>"
---

Rewrite $ARGUMENTS to essence, following the essence-doc skill: strip conversation residue and
inflated phrasing, keep only what the reader needs, preserve code/URLs/paths byte-for-byte, back
up the original, validate, then report.
