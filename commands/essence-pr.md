---
description: Write a PR description that stands on its own (bug + fix, nothing else)
argument-hint: "[PR number or branch]"
---

Write a PR description for $ARGUMENTS (or the current branch's diff if nothing is given),
following the essence-pr skill: bug + fix only, no conversation residue, no padding.
